<?xml version="1.0" encoding="utf-8"?>
<!--
  Politique reseau KORA :
  HTTPS obligatoire sur Internet, mais on autorise le HTTP en clair vers
  notre backend de dev (Wi-Fi local + emulateur Android). En production,
  le APK release devra pointer vers une URL HTTPS et ce fichier deviendra
  vide ou ne sera pas reference.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">192.168.1.12</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
